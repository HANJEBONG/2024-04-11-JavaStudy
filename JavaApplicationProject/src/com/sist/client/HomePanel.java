package com.sist.client;
import java.awt.Color;

import javax.swing.*;
public class HomePanel extends JPanel{
	public HomePanel() {
		setBackground(Color.DARK_GRAY);	// 배경을 파란색으로 설정합니다.
	}
}
